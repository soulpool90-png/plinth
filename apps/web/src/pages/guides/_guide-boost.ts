/** Additional per-slug content to reach 800+ words on every guide. */
export const GUIDE_BOOST: Record<string, string> = {
  "hugo-contact-form": `<h2>Version pinning and theme upgrades</h2><p>When you bump Hugo or swap themes, re-run a production form test. Themes sometimes ship their own contact partials with stale action URLs. Grep your <code class="inline-code">public/</code> output after <code class="inline-code">hugo --minify</code> to confirm the Plinth URL appears in built HTML. CI can fail the build if <code class="inline-code">formspree.io</code> or old endpoints remain in <code class="inline-code">public/</code>.</p>`,
  "11ty-contact-form": `<h2>Incremental adoption</h2><p>You do not need to convert every page at once. Add the Plinth form to the contact page first, leave legacy newsletter embeds until verified. Eleventy's incremental builds mean form partial changes rebuild quickly — iterate on honeypot placement and thank-you redirects without full-site regen pain.</p><h2>Directory structures</h2><p>Monorepos with <code class="inline-code">apps/docs</code> and <code class="inline-code">apps/marketing</code> can share one env-driven action URL or separate forms per app — Plinth form ids are free to mint. Document which id maps to which site in your team wiki.</p>`,
  "nextjs-contact-form": `<h2>Internationalization</h2><p>Localized contact pages should translate labels and error copy but keep the same Plinth action URL unless regulatory separation requires distinct forms per region. Hidden <code class="inline-code">locale</code> field helps inbox triage.</p><h2>Analytics</h2><p>Thank-you page views in Plausible or GA confirm successful submits when you use <code class="inline-code">_redirect</code> — compare against Plinth inbox counts to detect client-side blockers or ad-block interference.</p>`,
  "test-stripe-webhooks": `<h2>Dashboard delivery logs</h2><p>Stripe's dashboard shows delivery attempts alongside Catch events — correlate timestamps when debugging delays. Webhook signing secrets differ per endpoint; rotating endpoint URL to a new Catch bin requires updating the secret in your app env.</p><h2>Load testing</h2><p>Burst test mode checkouts are unnecessary for most indie products; one captured event per type suffices for handler development.</p>`,
  "test-github-webhooks": `<h2>Fine-grained permissions</h2><p>GitHub Apps use different delivery mechanics than repo webhooks — this guide focuses on classic repo hooks. App installations may require org admin approval before events flow.</p><h2>Workflow dispatch</h2><p>Some teams mirror webhook payloads into Actions via <code class="inline-code">repository_dispatch</code> — Catch remains useful to capture raw payloads before workflow YAML hardens.</p>`,
  "llm-json-repair": `<h2>Streaming responses</h2><p>Streaming completions may arrive in chunks — buffer full text before repair. Partial JSON mid-stream is expected; do not call repair until the model signals completion or timeout.</p><h2>Unicode and escapes</h2><p>Models occasionally double-escape quotes — repair handles common cases; log unrecoverable text for manual review.</p>`,
  "structured-output-json-schema": `<h2>Versioning schemas</h2><p>Attach <code class="inline-code">schema_version</code> in your validated object when evolving APIs — reject unknown versions in application code even when JSON Schema passes.</p><h2>Nullable fields</h2><p>Use <code class="inline-code">type: ["string", "null"]</code> explicitly when null is valid — models omit keys vs null inconsistently.</p>`,
  "webhook-replay": `<h2>Header fidelity</h2><p>Replay sends stored headers — confirm your handler does not require headers stripped or redacted by Catch. Authorization headers are redacted at capture; replay may omit secrets by design.</p><h2>Multiple targets</h2><p>Replay one event at a time to different URLs when comparing staging and local handlers — document which URL received which replay in notes.</p>`,
  "open-core-form-backend": `<h2>Worker limits</h2><p>CPU time per submission is tiny for screening — batch exports of spam logs for analysis should run in Cron Triggers, not inline on POST.</p><h2>Multi-tenant</h2><p>Agency Workers should namespace D1 rows by tenant id — never leak submissions across clients in shared tables.</p>`,
  "mcp-json-repair": `<h2>Local vs remote MCP</h2><p>Plinth MCP is remote HTTP — no local install. Latency is acceptable for agent tool calls; batch jobs should use npm library instead.</p><h2>Tool selection</h2><p>Prefer <code class="inline-code">schema_repair_and_validate</code> when schema is known — one round trip vs two tools.</p>`,
  "formspree-free-tier-limit": `<h2>Notification email</h2><p>Plinth Pro supports notify email and webhooks — map your Formspree notification expectations to webhook consumers early in migration.</p><h2>Spam comparison</h2><p>Run parallel honeypot config before cutover — compare spam rates for a week if Formspree spam filtering masked bot volume.</p>`,
  "webhook-site-alternative-free": `<h2>Collaboration</h2><p>Export Catch JSON into gist or ticket attachments — teammates without Plinth accounts can still review payloads offline.</p><h2>Rate limits</h2><p>High-volume load tests should not target anonymous bins — use dedicated test harnesses or Pro quotas per <a href="/pricing">Pricing</a>.</p>`,
  "python-true-in-json": `<h2>Historical models</h2><p>Older chat models skew Pythonic; newer models skew JSON — keep fixtures from both eras in tests when supporting multiple providers.</p><h2>Single-quoted keys</h2><p>Repair also normalizes quote styles where possible — pair with trailing comma guide for full cleanup.</p>`,
  "trailing-comma-json-fix": `<h2>Generator settings</h2><p>Some code formatters teach models that trailing commas are fine — reinforce JSON-only output in system prompts for extraction tasks.</p><h2>Array length</h2><p>Very long arrays with commas only at end still fail — repair walks entire structure.</p>`,
  "netlify-forms-alternative": `<h2>DNS cutover</h2><p>Form migration can happen before DNS moves — test Plinth on staging subdomain while Netlify still serves production until flip day.</p><h2>Build plugins</h2><p>Remove Netlify-specific build plugins that processed forms — they add build time without benefit on other hosts.</p>`,
  "stripe-webhook-signature-test": `<h2>Multiple endpoints</h2><p>Separate Catch bins for Connect vs direct charges if you operate both — secrets differ per endpoint in Stripe dashboard.</p><h2>Version tolerance</h2><p>Stripe API version in dashboard affects payload shape — note version in captured fixture metadata.</p>`,
  "github-webhook-payload-inspect": `<h2>Sponsored events</h2><p>Some enterprise payloads include fields absent in public docs — treat Catch captures as schema documentation for your org.</p><h2>Comment payloads</h2><p>Issue comment webhooks embed large HTML bodies — size limits may apply on replay targets.</p>`,
  "llm-markdown-json-fence": `<h2>Partial fences</h2><p>Models sometimes open a fence without closing — repair may fail; retry model with explicit “close the code block” instruction.</p><h2>Multiple JSON blocks</h2><p>When model returns two fenced blocks, specify in prompt which block is authoritative or take first parseable object.</p><h2>Chat UI copy</h2><p>Strip fences before displaying JSON in UI components — users should see structured data, not markdown artifacts.</p>`,
  "json-schema-validate-openai": `<h2>Batch API</h2><p>OpenAI batch jobs output large JSONL files — run repair+validate per line in map step before reducing results.</p><h2>Tool choice</h2><p>When both function calling and JSON mode exist, pick one per task — mixing increases fence and comma drift.</p>`,
  "catch-bin-expire": `<h2>Naming bins</h2><p>Use descriptive bin names in API create body — <code class="inline-code">stripe-checkout-sprint-42</code> beats <code class="inline-code">test1</code> when reviewing event lists weeks later.</p><h2>Alerting</h2><p>No built-in expiry alerts on free tier — calendar reminder to export before TTL if mid-debug.</p>`,
  "honeypot-spam-form": `<h2>Custom field names</h2><p>Pro may allow custom honeypot names — update partials everywhere when renaming from <code class="inline-code">_gotcha</code>.</p><h2>Autofill attributes</h2><p>Use <code class="inline-code">autocomplete="off"</code> on honeypot — some browsers ignore it; positioning off-screen remains primary defense.</p>`,
  "polar-billing-api-keys": `<h2>Refunds and churn</h2><p>Cancelled subscriptions eventually revoke API entitlements — handle 403 gracefully in scripts with message to renew on <a href="/pricing">Pricing</a>.</p><h2>Separate environments</h2><p>Use separate keys for staging and production Plinth resources when testing Pro replay — avoid replaying into prod accidentally.</p>`,
};
